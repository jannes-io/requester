// Extend default console logging
/* eslint-disable no-console */

const logger = {
  debug: console.debug,
  log: console.log,
  warn: console.warn,
  error: console.error,
};

export default logger;
